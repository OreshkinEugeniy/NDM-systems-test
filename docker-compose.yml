services:
  nginx1:
    image: nginx
    ports:
      - "81:80"
    volumes:
      - /home/oreshkinem/tests/ndm_systems/nginx_confs/nginx1.conf:/etc/nginx/conf.d/default.conf

  nginx2:
    image: nginx
    ports:
      - "82:80"
    volumes:
      - /home/oreshkinem/tests/ndm_systems/nginx_confs/nginx2.conf:/etc/nginx/conf.d/default.conf

  nginx3:
    image: nginx
    ports:
      - "83:80"
    volumes:
      - /home/oreshkinem/tests/ndm_systems/nginx_confs/nginx3.conf:/etc/nginx/conf.d/default.conf
  app:
    build: ./app
